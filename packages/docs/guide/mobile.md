# Mobile Support (Removed)

> **Mobile-specific layouts have been removed.** The dedicated mobile components (bottom tab bar, swipe-to-approve, mobile agent cards, mobile command sheet) were removed in v0.5.0.
>
> Flightdeck's desktop UI remains responsive and usable on tablets and larger screens. The PWA install capability is still available for browsers that support it.
