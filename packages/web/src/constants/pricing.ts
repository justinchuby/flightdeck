/**
 * DEPRECATED: Cost estimation removed — different Copilot models have
 * different pricing that we can't accurately estimate.
 * Use token counts (totalInputTokens / totalOutputTokens) instead.
 */
