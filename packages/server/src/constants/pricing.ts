/**
 * DEPRECATED: Cost estimation removed from analytics — different Copilot models
 * have different pricing that we can't accurately estimate.
 *
 * Per-token constants retained for BudgetEnforcer only.
 */
export const INPUT_COST_PER_TOKEN = 3.0 / 1_000_000;   // ~$3/MTok
export const OUTPUT_COST_PER_TOKEN = 15.0 / 1_000_000;  // ~$15/MTok
